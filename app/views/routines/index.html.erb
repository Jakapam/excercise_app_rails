<h1>Routines for <%= @user.username.capitalize %></h1>

<% @routines.each do |routine| %>
<%= link_to "#{routine.name}", user_routine_path(@user, routine) %><br>
<%end%>

<% if @user.id == session[:user_id]%>
  <%= link_to 'Add Routine', new_user_routine_path(@user) %><br>
<%end%>
